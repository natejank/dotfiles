#!/bin/bash

case "$(uname)" in
  "Linux")
    if [[ $(cat /sys/class/power_supply/BAT0/status) = "Discharging" ]]; then
      exit 1
    else
      exit 0
    fi
    ;;
  "Darwin")
    if [[ $(pmset -g ps | head -1) =~ "AC Power" ]]; then
      exit 0
    else
      exit 1
    fi
    ;;
esac

